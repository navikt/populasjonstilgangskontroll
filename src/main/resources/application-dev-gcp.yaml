management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    env:
        show-values: always
    configprops:
        show-values: always
  httpexchanges:
    recording:
      enabled: true
