management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    env:
        show-values: always
    configprops:
        show-values: always
    health:
      show-details: always
      show-components: always
  httpexchanges:
    recording:
      enabled: true
