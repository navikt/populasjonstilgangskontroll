pdl:
  scope: "api://${env}-fss.pdl.pdl-api/.default"
  base-uri: "https://${pdl}/graphql"
skjerming:
  base-uri: "http://skjermede-personer-pip.nom"
  scope: "api://${env}-gcp.nom.skjermede-personer-pip/.default"
graph:
  base-uri: https://www.todo.no