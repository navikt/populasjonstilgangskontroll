spring:
  cache:
    type: simple
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9999/jwks

graph:
  scope: https://graph.microsoft.com/.default
  base-uri: https://graph.microsoft.com/v1.0/

pdlgraph:
  scope: "api://dev-fss.pdl.pdl-api/.default"
  base-uri: "http://localhost:8081/graphql"

pdl:
  scope: "api://dev-fss.pdl.pdl-pip-api/.default"
  base-uri: "http://localhost:8081"

skjerming:
  base-uri: "http://localhost:8082"
  scope: "api://dev-gcp.nom.skjermede-personer-pip/.default"

entra-proxy:
  scope: "api://dev-gcp.sikkerhetstjenesten.entra-proxy/.default"
  base-uri: "http://localhost:8083"
