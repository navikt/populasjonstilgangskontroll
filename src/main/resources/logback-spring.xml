<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false" scan="false">
    <!-- Shutdown hook: flush and close appenders gracefully on JVM shutdown -->
    <shutdownHook class="ch.qos.logback.core.hook.DelayingShutdownHook">
        <delay>5000</delay>
    </shutdownHook>
    <!-- Logback status listener: logs Logback internal errors to stderr on startup issues -->
    <statusListener class="ch.qos.logback.core.status.OnConsoleStatusListener" />
    <!-- JSON console appender (prod/dev) -->
    <appender name="console-json" class="ch.qos.logback.core.ConsoleAppender">
        <immediateFlush>false</immediateFlush>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
    </appender>

    <!-- Text console appender (local/test) -->
    <appender name="console-text" class="ch.qos.logback.core.ConsoleAppender">
        <immediateFlush>false</immediateFlush>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %X{Nav-CallId} [%thread] %-5level %logger{70} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Nais secure log appender -->
    <appender name="secure-json" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>team-logs.nais-system:5170</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>
                {"google_cloud_project":"${GOOGLE_CLOUD_PROJECT}","nais_namespace_name":"${NAIS_NAMESPACE}","nais_pod_name":"${HOSTNAME}","nais_container_name":"${NAIS_APP_NAME}"}
            </customFields>
            <includeContext>false</includeContext>
        </encoder>
    </appender>

    <!-- Secure log text appender (local/test) -->
    <appender name="secure-text" class="ch.qos.logback.core.ConsoleAppender">
        <immediateFlush>false</immediateFlush>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %X{Nav-CallId} [%thread] %-5level %logger{70} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Async wrapper for Nais secure logs (prod/dev) - prevents blocking on network I/O -->
    <appender name="secure-async" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>false</includeCallerData>
        <maxFlushTime>30000</maxFlushTime>
        <neverBlock>false</neverBlock>
        <appender-ref ref="secure-json"/>
    </appender>

    <!-- Fallback stderr appender for logging Logback errors -->
    <appender name="stderr" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <immediateFlush>true</immediateFlush>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- OpenTelemetry wrapper (prod/dev) -->
    <appender name="otel" class="io.opentelemetry.instrumentation.logback.mdc.v1_0.OpenTelemetryAppender">
        <appender-ref ref="console-json" />
    </appender>

    <springProfile name="dev">
        <logger name="io.lettuce.core.protocol.ReconnectionHandler" level="warn"/>
        <logger name="io.lettuce.core.protocol.ConnectionWatchdog" level="warn"/>
        <logger name="org.hibernate.orm.connections.pooling" level="warn"/>
        <logger name="no.nav.tilgangsmaskin" level="trace"/>
        <logger name="no.nav.tilgangsmaskin.felles.rest.RestLoggingRequestInterceptor" level="trace"/>
        <logger name="secureLog" level="info" additivity="false">
            <appender-ref ref="secure-async"/>
        </logger>
        <root level="info">
            <appender-ref ref="otel"/>
        </root>
    </springProfile>

    <springProfile name="local | test">
        <logger name="no.nav.tilgangsmaskin" level="trace"/>
        <logger name="no.nav.tilgangsmaskin.felles.rest.RestLoggingRequestInterceptor" level="trace"/>
        <logger name="secureLog" level="info" additivity="false">
            <appender-ref ref="secure-text"/>
        </logger>
        <root level="info">
            <appender-ref ref="console-text" />
        </root>
    </springProfile>

    <springProfile name="prod">
        <turboFilter class="ch.qos.logback.classic.turbo.MarkerFilter">
            <Name>CONFIDENTIAL_FILTER</Name>
            <Marker>CONFIDENTIAL</Marker>
            <OnMatch>DENY</OnMatch>
        </turboFilter>
        <logger name="io.lettuce.core.protocol.ReconnectionHandler" level="warn"/>
        <logger name="io.lettuce.core.protocol.ConnectionWatchdog" level="warn"/>
        <logger name="org.hibernate.orm.connections.pooling" level="warn"/>
        <logger name="org.springframework.boot.autoconfigure.orm.jpa.JpaBaseConfiguration" level="error"/>
        <logger name="secureLog" level="info" additivity="false">
            <appender-ref ref="secure-async"/>
        </logger>
        <root level="info">
            <appender-ref ref="otel"/>
        </root>
    </springProfile>
    <!-- Global logger suppression (all profiles) -->
    <logger name="org.apache.hc.client5.http.impl.classic.HttpRequestRetryExec" level="warn"/>
    <logger name="org.springdoc.core.events.SpringDocAppInitializer" level="error"/>
    <logger name="org.springframework.boot.webmvc.autoconfigure.ProblemDetailsExceptionHandler" level="error"/>
</configuration>